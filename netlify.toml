# Build settings for Netlify.
# The Netlify Next.js runtime is auto-detected from package.json; no extra
# plugin or redirect config is required for a standard App Router build.
[build]
  command = "npm run build"
  publish = ".next"
