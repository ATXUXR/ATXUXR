# Build settings for Netlify.
# The Netlify Next.js runtime auto-detects the framework from package.json
# and handles the publish directory + serverless function routing — don't
# set `publish` manually or it'll serve `.next` as raw static files.
[build]
  command = "npm run build"
